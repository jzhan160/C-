/*copy control: copy constructor
                copy assignment operator
				move constructor
				move assignment operator
				destructor
//The copy and move constructors define what happens when an object is
//initialized from another object of the same type.
//The copy - and move - assignment operators define what happens when 
//we assign an object of a class type to another object of that same class type.
//The destructor defines what happens when an object
//of the type ceases to exist.
*/
#include "stdafx.h"
/*
Copy initialization happens not only when we define variables using an =, but also
when we
• Pass an object as an argument to a parameter of nonreference type
• Return an object from a function that has a nonreference return type
• Brace initialize the elements in an array or the members of an aggregate class
*/

int main()
{
    return 0;
}

